version: "3.8"

services:
  web:
    build: .
    container_name: construction-mgmt
    restart: unless-stopped
    ports:
      - "8080:5001"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - TZ=America/Chicago
